<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="bottom"
    android:padding="8dp">

    <TextView
        android:id="@+id/tv_chat_received"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:background="@drawable/right_message_text_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:maxWidth="@dimen/left_msg_max_width"
        android:padding="14dp"
        android:textColor="@color/white"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/tv_chat_time_received"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toStartOf="@+id/tv_chat_received"
        app:layout_constraintTop_toTopOf="@+id/tv_chat_received"
        app:layout_constraintBottom_toBottomOf="@+id/tv_chat_received"
        android:gravity="end"
        android:layout_marginEnd="7dp"
        android:textSize="12sp"
        android:textColor="@color/white"
        android:alpha="0.5"/>
</androidx.constraintlayout.widget.ConstraintLayout>
